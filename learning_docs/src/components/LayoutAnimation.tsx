'use client';

const LayoutAnimation = () => {
  return (
    <fieldset>
      <legend>Layout Animation</legend>
      <div id="layoutAnimationContainer"></div>
    </fieldset>
  );
};

export default LayoutAnimation;
