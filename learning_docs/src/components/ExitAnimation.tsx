'use client';

const ExitAnimation = () => {
  return (
    <fieldset>
      <legend>Exit Animation</legend>
      <div id="exitAnimationContainer"></div>
    </fieldset>
  );
};

export default ExitAnimation;
