'use client';

const EnterAnimation = () => {
  return (
    <fieldset>
      <legend>Enter Animation</legend>
      <div id="enterAnimationContainer"></div>
    </fieldset>
  );
};

export default EnterAnimation;
