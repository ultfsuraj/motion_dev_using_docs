'use client';

const PathDrawing = () => {
  return (
    <fieldset>
      <legend>Path Drawing</legend>
      <div id="pathDrawingContainer"></div>
    </fieldset>
  );
};

export default PathDrawing;
