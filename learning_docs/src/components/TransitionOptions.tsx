'use client';

const TransitionOptions = () => {
  return (
    <fieldset>
      <legend>Transition Options</legend>
      <div id="transitionOptionsContainer"></div>
    </fieldset>
  );
};

export default TransitionOptions;
